using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;

namespace getsetcode.Model.Enum
{
    public enum NavSection
    {
        Home, CV, Contact, Skills, Portfolio, Testimonials, Blog
    }

    public enum MessageType
    {
        Default, Error, Warning, Info, Success
    }

    public enum ContractStatus
    {
        Available, EndingSoon, Unavailable
    }
}
