@using getsetcode.Presentation.MvcHelpers
@model System.String
                    <img src="@Url.ContentImageQuoteOpen()" alt="Open quote" />
                    @Model
                    <img src="@Url.ContentImageQuoteClose()" alt="Close quote" />